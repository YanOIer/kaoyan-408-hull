# CO-03 Cache 与存储层次

> 凸包顶点 · 组成原理 | 考频：5 次综合题（2013 / 2017 / 2020 / 2021 / 2025）| 难度：★★★★
> 定位：组原计算题第一大户，也是 CO×OS 跨科链路（Cache–主存–虚存–TLB）的中枢。

---

## 一、为什么它是凸包顶点

Cache 考频 5 次、几乎"隔年必考"，且每考必是完整计算大题。它向上承接虚存
（OS 内存管理），向下触及主存组织，是凸包右上区最"厚实"的顶点：
**Cache 题 = 地址划分 + 命中判断 + 性能计算**，三步走通了就是稳定 10 分。

---

## 二、核心知识清单

### 2.1 地址划分（一切的起点）

主存地址 = | 标记 Tag | 组号/行号 Index | 块内地址 Offset |

| 映射方式 | 地址结构 | 特点 |
|---------|---------|------|
| 直接映射 | 标记 + **行号** + 块内 | 每组 1 行，冲突最多，硬件最简单 |
| 全相联 | 标记 + 块内 | 任意放，冲突最少，比较器最贵 |
| 组相联 | 标记 + **组号** + 块内 | 折中；n 路 = 每组 n 行 |

**速算**：块内地址位数 = log₂(块大小)；组号位数 = log₂(组数)；剩下全是标记。
`组数 = Cache行数 / 相联度`，`Cache行数 = Cache数据容量 / 块大小`。

### 2.2 命中与性能计算

```
命中率 h = 命中次数 / 总访问次数
平均访问时间 Ta = h·Tc + (1−h)·Tm        （先访问 Cache，缺失再访主存）
              或 = Tc + (1−h)·Tm         （看题目是否重叠，读题！）
```

- Tc：Cache 存取时间；Tm：主存存取时间（或缺失损失）。
- **效率 e = Tc / Ta**。

### 2.3 替换算法与写策略

| 替换算法 | 实现 | 特点 |
|---------|------|------|
| 随机 | 简单 | 不公平 |
| FIFO | 队列 | 可能 Belady 异常 |
| **LRU** | 计数器/栈 | 命中率高，408 最常考 |
| LFU | 访问计数 | 开销大 |

写策略：
- **写直达 WT**：同时写 Cache 和主存（一致性好，常配写缓冲）；
- **写回 WB**：只写 Cache，**脏位=1** 的行被替换时才写回主存（省带宽）。

写缺失：写分配（先调入 Cache）vs 非写分配（直接写主存）。

### 2.4 跨科链路（408 特色综合点）

```
CPU 给出 VA ──TLB/页表──→ PA ──Cache 地址划分──→ 命中? 读数据 : 访主存
                ↑OS 内存管理        ↑CO 存储层次
```

虚拟 Cache / 物理 Cache、TLB 与 Cache 的访问次序，是 CO×OS 交界的高阶考点。

---

## 三、典型考法

1. **地址划分**：给容量/块大小/相联度，求 Tag/Index/Offset 各几位（必考）。
2. **命中过程追踪**：给一串地址访问序列，判断每次命中/缺失、画出最终 Cache 内容。
3. **性能计算**：求命中率、平均访问时间、加速比。
4. **综合**：Cache + 虚存 + 磁盘一条链（2020/2025 风格），跨科大题。

---

## 四、易错点

- ⚠️ Cache 容量题目给的是**数据部分**，不含标记位/有效位阵列（除非明说"总容量"）。
- ⚠️ 直接映射的"行号"和组相联的"组号"位数不同，别混。
- ⚠️ LRU 模拟时**每次访问（命中也算）都要更新**使用次序。
- ⚠️ 写回法替换时先检查脏位——脏行要写回主存，影响总时间。
- ⚠️ "按字节编址"还是"按字编址"决定块内地址位数，审题第一刀。

---

## 五、复习建议

1. 默写三种映射的地址划分流程，做到"给参数 30 秒出结果"；
2. 手工模拟 5 组访问序列（含 LRU），画出每步 Cache 状态；
3. 精做 2013/2017/2020/2021/2025 五年 Cache 综合题——它们是同一道题的五次换装。


---

## 例题精练（附答案）

### 例题 1：Cache 地址划分（必考基本功）

**题目**：主存 32 位地址，Cache 数据容量 64 KB，块大小 64 B，4 路组相联。求标记、组号、块内地址各多少位。

**参考答案**：
1. 块内地址 = log₂64 = **6 位**；
2. Cache 行数 = 64 KB ÷ 64 B = 1024 行；组数 = 1024 ÷ 4 = 256 组 → 组号 = log₂256 = **8 位**；
3. 标记 = 32 − 8 − 6 = **18 位**。

验算：18 + 8 + 6 = 32 ✅。

---

### 例题 2：命中率与平均访问时间

**题目**：Cache 存取时间 Tc = 10 ns，主存 Tm = 100 ns，命中率 95%。采用"先查 Cache、缺失再访主存"方式，求平均访问时间。

**参考答案**：
- 缺失时总耗时 = Tc + Tm = 110 ns（先白查一次 Cache）；
- Ta = 0.95 × 10 + 0.05 × 110 = 9.5 + 5.5 = **15 ns**；
- 等价公式：Ta = Tc + (1−h)·Tm = 10 + 0.05 × 100 = 15 ns ✅。

若命中率提升到 99%：Ta = 10 + 1 = 11 ns——**命中率每涨 1%，Ta 降 1 ns**，这就是 Cache 的意义。

---

### 例题 3：直接映射命中追踪

**题目**：直接映射 Cache 共 4 行，块大小 1 字。主存按字编址，依次访问地址 0, 8, 0, 6, 8。问每次是否命中？最终 Cache 内容？

**参考答案**：直接映射行号 = 地址 mod 4。

| 访问 | 映射行 | 结果 | 说明 |
|------|--------|------|------|
| 0 | 行 0 | 缺失 | 装入块 0 |
| 8 | 行 0（8 mod 4 = 0） | 缺失 | **冲突**，块 0 被挤出，装入块 8 |
| 0 | 行 0 | 缺失 | 又冲突，换回块 0（抖动！） |
| 6 | 行 2 | 缺失 | 装入块 6 |
| 8 | 行 0 | 缺失 | 块 8 又被换回 |

- 命中率 0/5——直接映射的**抖动**典型案例；
- 若改成 2 路组相联（2 组 × 2 行），0 和 8 同组但各占一路，后三次全命中——这就是"相联度"的价值。
